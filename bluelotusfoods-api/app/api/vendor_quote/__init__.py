# Create a new `vendor-quote` folder under the `api` directory
# Move the four API files into the new folder
# Update the import statements in `main.py` to reflect the new folder structure
# Add an `__init__.py` file to the new folder to make it a proper Python package

# Create an empty `__init__.py` file to make the folder a Python package.
